<div class="pt-4 d-flex flex-row-reverse {{$sectionClass}}">
    <button type="submit" class="btn btn-primary ms-1">{{__('general.submit')}}</button>
    @if($reset)
    <button type="reset" class="btn btn-label-secondary">{{__('general.reset')}}</button>
    @endif
</div>
